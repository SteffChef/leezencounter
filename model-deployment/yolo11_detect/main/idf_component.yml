## IDF Component Manager Manifest File
dependencies:
  espressif/coco_detect:
    version: ^0.1.0
    override_path: ../../coco_detect
  espressif/esp32_p4_function_ev_board_noglib:
    version: ^4.0.1
    rules:
    - if: target == esp32p4
  espressif/esp32_s3_eye_noglib:
    version: ^3.1.0~1
    rules:
    - if: target == esp32s3
  # espressif/arduino-esp32: ^3.3.0
  # espressif/arduino-esp32: ^3.3.0~1
  jgromes/radiolib: ^7.2.1
